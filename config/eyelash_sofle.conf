# Sleep after one hour (1*60*60*1000 ms)
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=3600000
CONFIG_ZMK_SLEEP=y

# --- Hardware clock (Nice!Nano etc.)
CONFIG_CLOCK_CONTROL_NRF_K32SRC_XTAL=y
CONFIG_CLOCK_CONTROL_NRF_K32SRC_30PPM=y

# --- RGB Underglow (WS2812)
CONFIG_WS2812_STRIP=y
CONFIG_ZMK_RGB_UNDERGLOW=y
CONFIG_ZMK_RGB_UNDERGLOW_EXT_POWER=y

# Turn underglow ON at boot (set to =n if you want it off by default)
CONFIG_ZMK_RGB_UNDERGLOW_ON_START=n

# Default to SOLID WHITE at boot
# (White = saturation 0; hue ignored when sat=0)
CONFIG_ZMK_RGB_UNDERGLOW_EFF_START=1     # 0=Solid, 1=Breathe, 2=Spectrum, 3=Swirl
CONFIG_ZMK_RGB_UNDERGLOW_HUE_START=0     # hue doesn't matter when SAT=0
CONFIG_ZMK_RGB_UNDERGLOW_SAT_START=0     # <- makes it white
CONFIG_ZMK_RGB_UNDERGLOW_BRT_START=90    # match your max below
CONFIG_ZMK_RGB_UNDERGLOW_BRT_MAX=90

# Auto-off behavior
CONFIG_ZMK_RGB_UNDERGLOW_AUTO_OFF_IDLE=y
CONFIG_ZMK_RGB_UNDERGLOW_AUTO_OFF_USB=y

# --- EC11 encoder
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# --- External power control
CONFIG_ZMK_EXT_POWER=y

# --- Pointing/mouse
CONFIG_ZMK_POINTING=y

# --- Bluetooth / HID
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
CONFIG_ZMK_HID_REPORT_TYPE_NKRO=n

# --- Per-key backlight (separate from underglow)
CONFIG_ZMK_BACKLIGHT=y
CONFIG_ZMK_BACKLIGHT_BRT_START=100
CONFIG_ZMK_BACKLIGHT_ON_START=y
CONFIG_ZMK_BACKLIGHT_AUTO_OFF_IDLE=n

# --- Scan debounce
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=8
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=8
